/**
 * The single source of truth for the product name.
 *
 * Never hardcode "Marginalia" anywhere else — import from here so a rename is
 * one edit, and so page titles, emails, and the UI can never drift apart.
 */
export const APP_NAME = "Marginalia";

/** One-line description, used in metadata and on the marketing page. */
export const APP_TAGLINE =
  "Ask questions about your documents. Every answer cites the passage it came from.";
